# Schedule69 vehicle assets

Put the full drivable base car here as:

`public/models/vehicles/player-car.glb`

Vite serves that file at runtime as `/models/vehicles/player-car.glb`.

Keep this version as the high-detail/player car with the working interior and rigged parts. We can derive traffic/NPC LOD versions from it later rather than destructively simplifying this source model.
